<table border="0">
  <tr>
    <% %w[Su M T W Th F S].each do |day| %>
      <th><%= day %></th>
    <% end %>
  </tr>
  <% if @marches[team][team_member] %>
    <tr>
      <% Date.today.beginning_of_week(:sunday).step(Date.today.end_of_week(:sunday)) do |day| %>
        <td>
          <% if @marches[team][team_member][day] && (@marches[team][team_member][day] > 4 || (1..5).include?(day.wday)) %>
            <i class="icon-circle" style="color:<%= amount_to_color(@marches[team][team_member][day]) %>" title="<%= @marches[team][team_member][day] %>"></i>
          <% end %>
        </td>
      <% end %>
    </tr>
  <% end %>
</table>
